redux -> A predictable state container for JS Apps
1. Predictable
2. centralized
3. Debuggable
4. Flexible
when our appication size is too bigger  to handle then be need redux for it 
In conetext API is a box where be store all state of function
Ex: gState:{..},
    fState:{..}

redux is state management liberary
In redus there is a redux store to store the state
a. isme sari state ek hi object state ke andar hoti h
Ex: state: {
    gState:{..},
    fState:{..}
}

b. State update logic -> ek reducer kaam ka functio hota ko update state
c. method to modify changes 
